/**
 * main.js — Global initialisation and shared UI helpers
 *
 * Functions to implement:
 *   - initApp()                  — called on DOMContentLoaded
 *   - showFlash(msg, type)       — display a temporary flash message
 *   - setLoading(btn, isLoading) — toggle spinner on a button
 *   - formatCurrency(amount)     — "KES 100.00"
 *   - formatDate(isoString)      — locale-aware date/time string
 */
