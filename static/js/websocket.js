/**
 * websocket.js — Socket.IO client wrapper
 *
 * Functions to implement:
 *   - connectSocket(namespace)           — initialise Socket.IO, return socket
 *   - onPaymentUpdate(socket, callback)  — listen for 'payment_update' events
 *   - onDeviceConnected(socket, callback)
 *   - onAdminNotification(socket, callback)
 *   - disconnectSocket(socket)
 */
