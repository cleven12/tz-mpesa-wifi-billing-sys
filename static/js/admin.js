/**
 * admin.js — Admin dashboard interactivity
 *
 * Functions to implement:
 *   - loadDashboardStats()            — GET /api/admin/dashboard, render cards
 *   - loadUsersTable(page)            — GET /api/admin/users, render table
 *   - loadPaymentsTable(page, status) — GET /api/admin/payments, render table
 *   - loadDevicesTable(page)          — GET /api/admin/devices, render table
 *   - suspendUser(userId)             — POST /api/admin/users/:id/suspend
 *   - blockDevice(deviceId)           — POST /api/admin/devices/:id/block
 *   - renderPagination(meta, loadFn)  — generic pagination control builder
 */
