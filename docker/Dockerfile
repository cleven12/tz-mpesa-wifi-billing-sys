# Dockerfile — Production image for the WiFi Billing System
FROM python:3.11-slim

WORKDIR /app

# TODO: Install system dependencies
# RUN apt-get update && apt-get install -y libpq-dev gcc && rm -rf /var/lib/apt/lists/*

# TODO: COPY requirements.txt .
# TODO: RUN pip install --no-cache-dir -r requirements.txt

# TODO: COPY . .

# TODO: ENV FLASK_ENV=production

EXPOSE 8000

CMD ["echo", "TODO: implement CMD — gunicorn --worker-class eventlet -w 1 -b 0.0.0.0:8000 wsgi:application"]
